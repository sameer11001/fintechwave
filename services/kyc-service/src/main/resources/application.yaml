server:
  port: 8082

spring:
  application:
    name: kyc-service

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: always
